<h1>"<em><%= @project.name %></em>" Project Page </h1>
<p>
  <strong>Type:</strong>
  <%= @project.type_is %>
</p>


<% if @project.most_recent_goal == nil %>
	<p>How far along are you in your writing? <br> This app uses goals to track your writing progress. </p>
	<%= link_to "Let's make some goals", new_project_goal_path(@project) %>
<% else %>
	<strong>Most Recent Goal:</strong> <%= @project.most_recent_goal.type_is %> >>> <%= link_to "Write Now!", new_project_entry_path(@project.id, :goal_id => @project.goals.last.id) %> <br>
<% end %>


	<% if @project.entries.empty? %>
		&nbsp;
	<% else %>
		<div class="stats">
			<h3>Stats</h3>
				<p>You've spent <%= @project.total_time_spent %> hours in total on "<em><%= @project.name %></em>"</p>
		</div>
		<h4><%= link_to "Project Goal History", project_goals_path(@project) %></h4>
		<%= link_to "Make a New Goal", new_project_goal_path(@project) %>
	<% end %>



<p>&nbsp;</p>
<%= link_to 'Edit This Project', edit_project_path(@project) %> |
<%= link_to 'All my Projects', projects_path %>
