
<%= image_tag @user.avatar.url(:thumb) %>

<p><strong>Name:</strong>
              <%= @user.name %>
          </p>
          <p>
            <strong>Email:</strong>
            <%= @user.email %> <%# best_in_place @user, :email, :type => :input %>

          </p>
          <p>
            <strong>Location:</strong>
            <%= @user.location %>
          </p>
          <p>
            <strong>What kind of writer are you? </strong> 
            <%= @user.tag_list %>
          </p>
           <%= link_to 'Edit My Profile', edit_user_path(@user), :remote => true %> 

        